# دليل تفعيل الربط السحابي اللحظي (Firebase) – ديوان ماركت فرع البداوي

تم بناء هذا النظام ليعمل **بدون أي سيرفر دائم وبدون أي تكاليف** عبر السحابة اللحظية من Google Firebase (الخطة المجانية الدائمة).

---

## 🟢 الوضع الافتراضي الحالي (يعمل فوراً دون أي إعداد)
- النظام الآن يعمل فوراً داخل المتصفح.
- يدعم التزامن اللحظي بين النوافذ والتبويبات المفتوحة تلقائياً عبر BroadcastChannel.
- يحفظ جميع البيانات والورديات والأرشيف محلياً على الجهاز (LocalStorage).

---

## ☁️ خطوات تفعيل التزامن عبر هواتف المشرفين على الإنترنت (خلال 3 دقائق)

لكي تظهر التحديثات فوراً على هواتف المشرفين (أحمد بدرة، عيسى ورور، سامح الحسن) أثناء تجولهم في الصالة من أجهزتهم المحمولة عبر 4G أو واي فاي الماركت:

### الخطوة 1: إنشاء حساب ومشروع مجاني في Firebase
1. ادخل إلى موقع [Firebase Console](https://console.firebase.google.com/) وسجل الدخول بحساب Google (Gmail).
2. اضغط على **Add project** أو **إنشاء مشروع**.
3. اكتب اسم المشروع (مثلاً: `diwan-baddawi-supervisor`)، ثم اضغط **Continue**.
4. يمكنك تعطيل Google Analytics (غير مطلوب)، ثم اضغط **Create project**.

### الخطوة 2: إنشاء قاعدة البيانات اللحظية (Realtime Database)
1. من القائمة الجانبية، اختر **Build** ثم اضغط على **Realtime Database**.
2. اضغط على **Create Database** (إنشاء قاعدة بيانات).
3. اختر الموقع الجغرافي الافتراضي ثم اضغط **Next**.
4. في شاشة إعدادات الأمان (Security Rules):
   - اختر **Start in test mode** (بدء في وضع الاختبار).
   - اضغط **Enable**.

### الخطوة 3: نسخ كود الإعداد (Web App Config)
1. اضغط على علامة الترس ⚙️ بجانب **Project Overview** في أعلى القائمة، ثم اختر **Project settings** (إعدادات المشروع).
2. انزل إلى أسفل الصفحة إلى قسم **Your apps**، واضغط على أيقونة الويب **`</>`**.
3. اكتب اسماً للتطبيق (مثلاً: `diwan-web`) ثم اضغط **Register app**.
4. سيظهر لك كود إعداد يشبه التالي:
```json
{
  "apiKey": "AIzaSy...",
  "authDomain": "diwan-baddawi-supervisor.firebaseapp.com",
  "databaseURL": "https://diwan-baddawi-supervisor-default-rtdb.firebaseio.com",
  "projectId": "diwan-baddawi-supervisor",
  "storageBucket": "diwan-baddawi-supervisor.appspot.com",
  "messagingSenderId": "...",
  "appId": "..."
}
```

### الخطوة 4: تفعيل الربط داخل التطبيق
1. افتح صفحة النظام `index.html` في المتصفح.
2. اضغط على زر الترس **⚙️** في أعلى يسار الصفحة.
3. الصق كود الـ JSON الذي نسخته داخل المربع.
4. اضغط على زر **"حفظ وتفعيل"**.
5. ستتحول الشارة في أعلى الصفحة فوراً إلى: `متزامن سحابياً ☁️` بنقطة خضراء نابضة.

---

## 📱 كيفية تثبيت التطبيق على هواتف المشرفين كأنه تطبيق رسمي

1. بعد رفع ملف `index.html` على أي استضافة مجانية سريعة (مثل GitHub Pages أو Vercel أو Netlify):
2. يفتح المشرف الرابط على متصفح هاتفه (Safari على iPhone أو Chrome على Android).
3. يضغط على زر المشاركة/خيارات المتصفح ويختار:
   - في Android: **"إضافة إلى الشاشة الرئيسية" (Add to Home screen)**.
   - في iPhone: **"إضافة إلى الشاشة الرئيسية" (Add to Home Screen)**.
4. سيظهر التطبيق كأيقونة على شاشة الهاتف تفتح بملء الشاشة دون شريط المتصفح لسهولة الاستخدام اليومي داخل الصالة.
